image: docker:latest
variables:
  IMAGE_TAG: latest
services:
  - docker:dind
stages:
  - build

build:
    stage: build
    script:
        - echo "Building imgae...."
        - docker build -t auth:latest .

    only:
        - main