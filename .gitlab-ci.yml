image: docker:latest
variables:
  DOCKER_TLS_CERTDIR: /certs
  IMAGE_TAG: latest
services:
  - docker:dind
stages:
  - build

build:
    stage: build
    script:
        - echo "Building imgae...."
        - docker build -t auth:latest .

    only:
        - main