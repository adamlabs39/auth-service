const REQUIRED = "tidak boleh kosong";
const passwordRequired = `password ${REQUIRED}, password tidak boleh kurang dari 8 karakter`;
const usernameRequired = `username ${REQUIRED}`;
const categoryRequired = `category ${REQUIRED}`;
const mainMenuCodeRequired = `mainMenuCode ${REQUIRED}`;
const mainMenuNameRequired = `mainMenuName ${REQUIRED}`;
const mainMenuRequired = `mainMenu ${REQUIRED}`;
const subMenuCodeRequired = `subMenuCode ${REQUIRED}`;
const subMenuNameRequired = `subMenuName ${REQUIRED}`;
const statusRequired = `status ${REQUIRED}`;
const faskesUuidRequired = `faskesUuid ${REQUIRED}`;
const uuidRequired = `uuid ${REQUIRED}`;
const permisionUuidRequired = `permisionUuid ${REQUIRED}`;
const codeRequired = `code ${REQUIRED}`;
const nameRequired = `name ${REQUIRED}`;
const emailRequired = `email ${REQUIRED}`;
const dokterUuidRequired = `dokter ${REQUIRED}`;
const phoneRequired = `phone ${REQUIRED}`;
const iventoryMedisRequired = `iventoryMedis ${REQUIRED}`;
const iventoryNonMedisRequired = `iventoryNonMedis ${REQUIRED}`;
const roleRequired = `role ${REQUIRED}`

export {
  passwordRequired,
  categoryRequired,
  codeRequired,
  dokterUuidRequired,
  emailRequired,
  faskesUuidRequired,
  iventoryMedisRequired,
  iventoryNonMedisRequired,
  mainMenuCodeRequired,
  mainMenuNameRequired,
  mainMenuRequired,
  nameRequired,
  permisionUuidRequired,
  phoneRequired,
  statusRequired,
  subMenuCodeRequired,
  uuidRequired,
  subMenuNameRequired,
  usernameRequired,
  roleRequired
};
